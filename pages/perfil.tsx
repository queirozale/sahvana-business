import React, { useState } from 'react';
import Header from "../components/Header/Header";

const Profile = () => {
  return (
    <div>
      <Header />
      <h1>Perfil</h1>
    </div>
  );
};

export default Profile;